{
  "name": "google-searchbar-jump-fix",
  "version": "0.0.1",
  "description": "Pressing '/' will always jump to the google search bar.",
  "author": "Kulsgam",
  "scripts": {
    "build": "npx tsc && npx tailwindcss build -i src/tailwind.css -o out/css/tailwind.css"
  },  
  "devDependencies": {
    "@types/chrome": "^0.0.243"
  },
  "dependencies": {
    "autoprefixer": "^10.4.15",
    "postcss": "^8.4.28",
    "tailwindcss": "^3.3.3"
  }
}
